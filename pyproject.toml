[project]
name = "loom-summarize"
version = "0.1.0"
description = "CLI tool to download Loom videos, transcribe locally, and summarize with Claude"
requires-python = ">=3.11"
dependencies = [
    "mlx-whisper",
    "yt-dlp",
]

[project.scripts]
loom-summarize = "loom_summarize:main"
